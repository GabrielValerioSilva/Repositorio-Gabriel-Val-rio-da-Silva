#include "data.h"
#include <stdio.h> // Substituir por uma biblioteca de banco de dados real

void inserirCliente(char *nome, int idade) {
    // Simulação de inserção em banco de dados
    printf("Cliente %s com %d anos cadastrado.\n", nome, idade);
}

//Camada de Acesso a Dados (Data Access Layer): Interage com o banco de dados, realizando operações de inserção, atualização, exclusão e consulta.